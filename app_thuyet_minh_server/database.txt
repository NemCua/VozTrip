erd 


app_thuyet_minh=# CREATE TABLE users (
    id              INT PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
    name            VARCHAR(150) NOT NULL,
    phone           VARCHAR(30),
    email           VARCHAR(255) NOT NULL UNIQUE,
    role            VARCHAR(30) NOT NULL, -- admin/seller/tourist
    pass_hash       VARCHAR(255) NOT NULL,
    status          INT NOT NULL DEFAULT 1,

    created_at      TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);
CREATE TABLE
app_thuyet_minh=# CREATE TABLE seller (
    id              INT PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
    owner_id        INT NOT NULL,
    shop_name       VARCHAR(200) NOT NULL,
    description     TEXT,
    address         VARCHAR(300),
    verified        BOOLEAN DEFAULT FALSE,

    created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    CONSTRAINT fk_seller_user
        FOREIGN KEY (owner_id) REFERENCES users(id)
        ON DELETE CASCADE
);
CREATE TABLE
app_thuyet_minh=# CREATE TABLE poi (
    id                      INT PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
    seller_id               INT NOT NULL,
    name                    VARCHAR(200) NOT NULL,
    description             TEXT,

    latitude                DECIMAL(9,6) NOT NULL,
    longitude               DECIMAL(9,6) NOT NULL,
    trigger_radius_meters   FLOAT DEFAULT 5,

    status                  INT DEFAULT 1,

    created_at              TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at              TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    CONSTRAINT fk_poi_seller
        FOREIGN KEY (seller_id) REFERENCES seller(id)
        ON DELETE CASCADE
);
CREATE TABLE
app_thuyet_minh=# CREATE TABLE media (
    id          INT PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
    poi_id      INT NOT NULL,
    url         TEXT NOT NULL,
    type        VARCHAR(50) NOT NULL, -- image/video

    CONSTRAINT fk_media_poi
        FOREIGN KEY (poi_id) REFERENCES poi(id)
        ON DELETE CASCADE
);
CREATE TABLE
app_thuyet_minh=# CREATE TABLE audio (
    id              INT PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
    audio_url       TEXT NOT NULL,
    duration_sec    INT,
    source_type     VARCHAR(30) -- upload / tts
);
CREATE TABLE
app_thuyet_minh=# CREATE TABLE narration (
    id          INT PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
    poi_id      INT NOT NULL,
    text        TEXT NOT NULL,
    language    VARCHAR(10) DEFAULT 'vi',
    audio_id    INT,
    status      INT DEFAULT 1,

    CONSTRAINT fk_narration_poi
        FOREIGN KEY (poi_id) REFERENCES poi(id)
        ON DELETE CASCADE,

    CONSTRAINT fk_narration_audio
        FOREIGN KEY (audio_id) REFERENCES audio(id)
        ON DELETE SET NULL
);
CREATE TABLE
app_thuyet_minh=# CREATE TABLE question (
    id              INT PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
    poi_id          INT NOT NULL,
    question_text   TEXT NOT NULL,
    sort_order      INT DEFAULT 0,
    status          INT DEFAULT 1,

    CONSTRAINT fk_question_poi
        FOREIGN KEY (poi_id) REFERENCES poi(id)
        ON DELETE CASCADE
);
CREATE TABLE
app_thuyet_minh=# CREATE TABLE question_answer (
    id              INT PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
    question_id     INT NOT NULL,
    answer_text     TEXT NOT NULL,
    language        VARCHAR(10) DEFAULT 'vi',
    audio_id        INT,

    CONSTRAINT fk_answer_question
        FOREIGN KEY (question_id) REFERENCES question(id)
        ON DELETE CASCADE,

    CONSTRAINT fk_answer_audio
        FOREIGN KEY (audio_id) REFERENCES audio(id)
        ON DELETE SET NULL
);
CREATE TABLE
app_thuyet_minh=# CREATE INDEX idx_poi_lat_lng ON poi(latitude, longitude);
CREATE INDEX idx_question_poi ON question(poi_id);
CREATE INDEX idx_narration_poi ON narration(poi_id);
CREATE INDEX
CREATE INDEX
CREATE INDEX
app_thuyet_minh=# 
Narravia
